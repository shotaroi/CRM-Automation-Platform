spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///crm_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

crm:
  jwt:
    secret: test-secret-key-min-256-bits-for-hs256-algorithm
    expiration-ms: 3600000
